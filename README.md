# personal-site

The source code for thomasjack.ca

## Commands

```bash
# Development
npm start           # Serve with hot reload
npm run build       # Build for production

# Deployment
./deploy.sh --dry-run  # Preview changes (dry run)
./deploy.sh            # Build and deploy to server
```
